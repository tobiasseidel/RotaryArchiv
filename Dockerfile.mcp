FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ /app/src/

RUN mkdir -p /app/data

EXPOSE 8001

CMD ["uvicorn", "src.rotary_archiv.mcp.sse_server:app", "--host", "0.0.0.0", "--port", "8001"]
